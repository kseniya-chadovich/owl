Start server with uvicorn … --reload.

Open Swagger UI at /docs.

Run POST /api/student-progress (paste sample JSON).

Run POST /api/schedules/generate (paste sample JSON).

Confirm you see ok: true and a list of schedules.